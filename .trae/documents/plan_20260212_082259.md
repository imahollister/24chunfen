针对您的需求，我将执行以下修改计划：

1.  **调整分享和规则按钮为竖向排列**
    *   修改 `src/pages/SpringEquinoxV2.tsx`，将右上角功能区的布局样式从水平 (`flex`) 改为垂直 (`flex-col`)，避免遮挡标题。

2.  **修复美食和好物副标题不显示的问题**
    *   **原因**：您的浏览器缓存（LocalStorage）中存储的是旧版数据，缺少 `subtitle` 字段。
    *   **方案**：在 `src/services/DataManager.ts` 中增加**自动修复逻辑**。当系统加载数据时，会自动检查并为旧数据补充缺失的副标题（如“山野之鲜”、“自动断电”等），确保移动端能正常显示。

执行后，请刷新页面即可看到竖排按钮和完整的副标题。